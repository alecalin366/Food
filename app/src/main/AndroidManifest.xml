<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.food">

    <application
        android:allowBackup="false"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Food">
        <activity android:name=".Profile.UserProfileActivity"></activity>
        <activity android:name=".Recipe.RecipeFiltredByCategory" />
        <activity
            android:name=".Recipe.DetailedRecipe"
            android:label="@string/title_activity_detailed_recipe" />
        <activity android:name=".ShopActivity.ShopActivity" />
        <activity android:name=".Recipe.AddRecipeActivity" />
        <activity android:name=".Likes.LikesActivity" />
        <activity android:name=".Search.SearchActivity" />
        <activity android:name=".Profile.AccountSettingsActivity" />
        <activity android:name=".Login.RegisterActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Onboarding.StartupActivity1" />
        <activity android:name=".Onboarding.StartupActivity3" />
        <activity android:name=".Onboarding.StartupActivity2" />
        <activity android:name=".Onboarding.WelcomeActivity" />
        <activity android:name=".Home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- <activity -->
        <!-- android:name="com.yalantis.ucrop.UCropActivity" -->
        <!-- android:screenOrientation="portrait" -->
        <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar"/> -->
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>