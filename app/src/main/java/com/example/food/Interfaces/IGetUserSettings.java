package com.example.food.Interfaces;

import com.example.food.User.User;
import com.example.food.User.UserSettings;

public interface IGetUserSettings {
    void getUserSettings(User userSettings);
}
